echo "Running staged file checks..."

pnpm lint-staged
status=$?

if [ $status -ne 0 ]; then
  echo ""
  echo "❌ Pre-commit checks failed"
  echo "Fix the lint or formatting errors above, then stage the changes and commit again."
  exit $status
fi

echo "✅ Staged files look good"
