# Changesets

This directory manages versioning and changelogs for Static UI packages.

## How to add a changeset

Run `pnpm changeset` in the root of the repository and follow the prompts.

## Publishing

Run `pnpm changeset version` to bump versions and generate changelogs, then `pnpm publish -r` to publish.
